{% extends '::base.html.twig' %}

{% block body %}

<div class="container main">
	<div class="row">
		<div class="span12">
			<h1>Edit Skills</h1>
			
			<form method="post" class="form form-horizontal">
				<div class="columns">
				    {% for skill in skills %}
				    	<div class="control-group">
				    		<label for="" class="control-label">{{ skill.skillname }}</label>
				    		<div class="controls">
				    			<input type="checkbox" name="skills[{{ skill.id }}]" {% if recruit.hasSkill(skill) %}checked="true"{% endif %} />
			    			</div><!-- controls -->
			    		</div><!-- control-group -->
				    {% endfor %}
			    </div><!-- skills-holder -->
			
			    <input type="submit" class="crus-btn" />
			</form>
		</div><!-- span12 -->
	</div>
</div><!-- container -->

{% endblock %}